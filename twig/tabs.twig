<!DOCTYPE html>
<html>
<head>
    <title>{{ title }} Tabs</title>
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:400,400italic,700italic,700|Source+Code+Pro" rel="stylesheet" type="text/css">
    <link href="misfits.css" rel="stylesheet" type="text/css">
</head>
<body>
    <h1>{{ title }}</h1>
    {% for version in versions %}
    {% if version.version %}
    <h2>{{ version.version }}</h2>
    {% endif %}
    <pre>{{ version.tabs }}</pre>
    {% endfor %}
</body>
</html>
